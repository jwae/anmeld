DB_HOST=localhost
DB_PORT=3307
DB_NAME=anmeld
# DB-User mit SChreibrechten auf die DB stats
# Muss in MariadB oder mit dBeaver angelegt werden
# Über diesen User greift das Backend auf dei DB zu
DB_USER=anmeld_user
DB_PASSWORD=Admin1234567
JWT_SECRET=supersecretkey
OPENROUTESERVICE_API_KEY=
# Wandelt Adressen in Koordinaten um und berechnet 
# die Entfernung (über Strasse) zur Schule. (Nicht Luftlinie!)
# Falls ORS nicht erreichbar ist, fallback auf mathematische 
# der Luftlinie anhand der Koordinaten. 